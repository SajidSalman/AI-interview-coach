import express from "express";
const router = express.Router();

// This file is intentionally left minimal to prevent
// AI library conflicts with geminiController.js.
// You can add any non-AI resume routes here if needed.

export default router;